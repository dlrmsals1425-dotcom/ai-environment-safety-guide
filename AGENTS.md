# AI 환경안전 지도: 팀 개발 지침

- 프로젝트 루트는 이 파일이 있는 디렉터리다. 앱 명령은 `app/`에서 실행한다. 특정 개발자의 절대 경로를 가정하지 않는다.
- `README.md`, `CONTRIBUTING.md`, `DATA_SOURCES.md`, 최신 `docs/06`~`docs/12`를 읽는다. 초기 기획 문서는 작성 당시의 기록이다.
- 대상은 서울특별시이며 MapLibre + OSM 기반이다. 덕양구/기존 빛길 결과를 서울 성능 수치와 섞지 않는다.
- 원래 빛길 프로젝트와 실행 중인 별도 서버를 수정·중단하지 않는다.
- GitHub 저장소는 비공개다. `main`은 공유/배포, `develop`은 개발 통합, 개별 변경은 `feature/*`/`fix/*`에서 PR로 검토한다.
- 사용자가 요청한 GitHub 저장소 생성·코드 게시·협업 설정은 허용된 작업이다. 팀원 초대는 현재 보류 요청 상태다. 계정·비밀키·권한 범위를 임의로 확대하지 않는다.
- 현재 서울 앱의 키는 필요하지 않다. `.env`, 토큰, 개인 작업 기록, 원본·전체 가공 자료는 Git에 올리지 않는다. 전체 가공 자료는 검증된 Release ZIP으로만 관리한다.
- 팀원이 받은 기본 자료는 실제 데이터의 3개 구역 샘플이다. 샘플 범위 밖을 완전한 서울 분석으로 표시하지 않는다.
- 추정 지면은 약 30m급 DSM 가공값이며 정밀 DTM이 아니다. 기본 2m/경계 0.5m는 계산 간격이지 현장 정확도가 아니다.
- 건물 높이의 대장값/층수 추정/미상, 수목 자료의 2013년 시점, 제설기지 좌표의 추론 상태를 보존한다.
- 개별 수목 차폐, 기상, 위험도는 아직 미구현이다. 단순히 그늘=위험, 시설 많음=안전으로 점수화하지 않는다.
- 한 파일을 여러 작업자가 동시에 수정하지 않는다. 변경 범위와 담당을 먼저 구분한다.
- 변경에 맞는 테스트와 `npm run build`를 수행하고, 의미 있는 동작 변경은 실제 지도에서도 확인한다. 경로·상태·검증 결과를 사실대로 보고한다.
- 배포 준비와 실제 외부 사이트 배포를 구분한다. Vercel 유료 요금제 전환은 별도 사용자 결정이 필요하다.
