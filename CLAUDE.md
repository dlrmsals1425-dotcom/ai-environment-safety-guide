# Claude 팀 작업 지침

이 저장소의 `AGENTS.md`, `CONTRIBUTING.md`, 최신 구현 문서를 따른다.
앱은 `app/`에서 실행하고 `npm run dev`가 필요한 시연 자료를 준비한다.
현재 기준은 서울 MapLibre 지도이며 과거 Google Maps/코드 동결 기록을 현재 요구사항으로 적용하지 않는다.
수정 전 담당 파일·브랜치를 확인하고 다른 작업자의 변경을 덮어쓰지 않는다.
원천/전체 데이터, API 키와 개인 작업 기록은 커밋하지 않는다.
수치·정확도는 검증 근거가 있을 때만 쓰고, 미구현 범위를 숨기지 않는다.
